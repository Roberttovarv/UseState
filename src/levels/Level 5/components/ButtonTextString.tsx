import { ColoredText } from "../../../components/ColoredText"

export const ButtonTextString = () => (
        <ColoredText text={[["buttonText", "lightgreen"]]} style={{ display: "inline-flex" }} />
    )
